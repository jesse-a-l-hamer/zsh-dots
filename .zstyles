#!/bin/zsh
#
# .zstyles - Set zstyle settings for plugins that need them.
#

# Source anything in .zstyles.d.
for _rc in ${ZDOTDIR:-$HOME}/.zstyles.d/*.zsh; do
  # Ignore tilde files.
  if [[ $_rc:t != '~'* ]]; then
    source "$_rc"
  fi
done
unset _rc
